specVersion: "v1"
serviceId: "hotel-booking"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "hotel-booking-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
